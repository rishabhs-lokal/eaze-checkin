# Template only — do not commit a filled-in copy of this file.
# Create the real secret out-of-band instead, e.g.:
#   kubectl create secret generic eaze-api-secrets \
#     --namespace eaze \
#     --from-literal=DATABASE_URL='postgresql://user:pass@host:5432/eaze'
# or via your cluster's secrets manager (External Secrets, Sealed Secrets, etc).
apiVersion: v1
kind: Secret
metadata:
  name: eaze-api-secrets
  namespace: eaze
type: Opaque
stringData:
  DATABASE_URL: "postgresql://user:pass@host:5432/eaze"
